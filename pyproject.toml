[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "bilibili-data-crawler"
version = "0.1.0"
description = "Bilibili 视频信息、评论、字幕、弹幕和搜索采集工具"
requires-python = ">=3.9"
dependencies = [
    "playwright",
    "protobuf>=6.31.1,<7",
]

[project.optional-dependencies]
dev = [
    "grpcio-tools",
]

[project.scripts]
bilibili = "main:main"

[tool.setuptools]
py-modules = [
    "bilibili_api",
    "crawler_comment",
    "crawler_dm",
    "crawler_info",
    "crawler_search",
    "crawler_subtitle",
    "dm_pb2",
    "login",
    "main",
]
